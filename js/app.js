const translations={
  tr:{navHome:'Ana Sayfa',navPredictions:'Tahminler',navRankings:'Sıralama',navRules:'Kurallar',navProfile:'Profil',navLogin:'Giriş Yap',navSignup:'Üye Ol',navLogout:'Çıkış Yap',navStart:'Tahmine Başla',homeLabel:'Dünya Kupası 2026 · 104 Maç',homeTitle:'104 Maç.<br>48 Ülke.<br>Milyonlarca Futbolsever.<br><span class="htitle-a">Tek Dünya Kupası Arenası.</span>',homeSub:'Her maçı tahmin et, puan topla, ülkene katkı sağla ve Dünya Kupası 2026 boyunca dünyanın dört bir yanındaki futbolseverlerle aynı sıralamada yarış.',rankingsSee:'Sıralamayı Gör',statMatches:'Maç',statCountries:'Ülke',statDays:'Gün',statCompetition:'Rekabet',manifestLabel:'Manifestomuz',manifest1:'Dünya Kupası dört yılda bir gelir.',manifest2:'Farklı ülkelerden milyonlarca insan aynı maçları izler, aynı golleri kutlar ve aynı hüzünleri yaşar.',manifest3:'World Cup Arena, bu insanları ortak bir deneyimde buluşturur.',manifest4:'Bu sadece bir tahmin oyunu değil.',manifest5:'Bu, her puanın hem senin hem de temsil ettiğin ülkenin kaderini etkilediği küresel bir futbol arenası.',gameLabel:'Oyun',howTitle:'Nasıl Çalışır',how1Title:'Turnuva Tahminleri Yap',how1Desc:'İlk düdükten önce şampiyonu, finalistleri, gol kralını, asist kralını ve turnuvanın oyuncusunu tahmin et.',how2Title:'Maç Skorlarını Tahmin Et',how2Desc:'Her maçtan önce kesin skor tahminini gönder. Tahminin ne kadar yakınsa, puanın o kadar yüksek olur.',how3Title:'Puan Topla',how3Desc:'Doğru skorlar, doğru sonuçlar, tahmin serileri ve turnuva tahminlerinden puan kazan. Her maç önemlidir.',how4Title:'Ülkeni Temsil Et',how4Desc:'Puanların, seçtiğin ülkenin genel sıralamasına katkı sağlar. Her tahminde bayrağını taşı.',communityLabel:'Topluluk',socialTitle:'Sosyal Deneyim',joinNow:'Şimdi Katıl',ctaTitle:'Küresel Dünya Kupası<br><span class="cta-ta">deneyiminin parçası ol.</span>',ctaSub:'104 maç. Sayısız tahmin. Tek küresel arena.',predLabel:'Tahminler',predTitle:'Tahminler',warningLabel:'Önemli Uyarı',warningTitle:'Tahminlerini kaydetmeden önce kontrol et',warning1:'Turnuva tahminleri yalnızca bir kez yapılabilir. Kaydedildikten sonra değiştirilemez.',warning2:'Şampiyon, finalistler, gol kralı, asist kralı ve turnuvanın oyuncusu tahminlerini dikkatli seç.',warning3:'Maç skor tahminleri de her maç için yalnızca bir kez yapılabilir. Kaydetmeden önce skorunu kontrol et.',warning4:'Acele etme. Erken tahminler yanıltıcı olabilir.',tournamentPredictions:'Turnuva Tahminleri',tournamentGeneral:'Turnuva Genel Tahminleri',championQuestion:'Şampiyon kim olur?',finalist1:'Finalist 1',finalist2:'Finalist 2',topScorerQuestion:'Gol kralı kim olur?',topAssistQuestion:'Asist kralı kim olur?',playerTournamentQuestion:'Turnuvanın oyuncusu kim olur?',saveTournament:'Turnuva Tahminlerini Kaydet',goMatchPredictions:'Maç Tahminlerine Git',matchPredictions:'Maç Tahminleri',groupMatches:'Grup Aşaması Maçları',showAllMatches:'Tüm Maçları Göster',rankingsLabel:'Sıralama',rankingsTitle:'Sıralama',individualRanking:'Bireysel Sıralama',countryParticipation:'Ülke Katılımı',collectiveRanking:'Ülke / Kolektif Puan Sıralaması',showMore:'Daha Fazla Gör',showAllRanking:'Tüm Sıralamayı Göster',closeList:'Listeyi Kapat',points:'puan',participant:'katılımcı',user:'kullanıcı',selfTag:'Sen',rulesLabel:'Kurallar',rulesTitle:'Kurallar ve Puanlama',whatIs:'Dünya Kupası Arena Nedir?',howJoin:'Nasıl Katılırsın?',rulesMatch:'Maç Tahminleri',rulesTournament:'Turnuva Tahminleri',pointsSystem:'Puan Sistemi',countryPoints:'Ülke Puanı',socialShares:'Sosyal Paylaşımlar',premiumVisibility:'Premium Görünürlük',generalRules:'Genel Kurallar',importantWarning:'Önemli Uyarı',profileLabel:'Profil',profileTitle:'Profil',loginRequired:'Giriş Gerekli',loginRequiredText:'Profilini görmek için giriş yapmalısın.',profileCard:'Profil Kartı',username:'Kullanıcı adı',email:'E-posta',country:'Ülke',totalPoints:'Toplam puan',worldRank:'Dünya sırası',countryRank:'Ülke içi sırası',referralCode:'Davet kodu',premiumText:'Premium kullanıcılar sıralamada kendi seçtikleri isimle görünebilir, profil fotoğrafı ekleyebilir ve paylaşılabilir kullanıcı kartı oluşturabilir.',premiumUpgrade:'Premium’a Geç',customUsername:'Özel kullanıcı adı',profilePhoto:'Profil fotoğrafı',visibleName:'Sıralamada görünür isim',shareableCard:'Paylaşılabilir kullanıcı kartı',xTagChance:'X paylaşımlarında etiketlenme şansı',account:'Hesap',password:'Şifre',countrySelect:'Ülke seçimi',noAccount:'Hesabın yok mu? Üye ol',hasAccount:'Hesabın var mı? Giriş yap',navStartIcon:'⚽ Tahmine Başla',rulesWhatDesc:'Dünya Kupası Arena, Dünya Kupası boyunca maç ve turnuva tahminleri yaparak puan topladığın, ülkeni temsil ettiğin ve küresel sıralamada yarıştığın bir tahmin ve topluluk deneyimidir.',rulesJoinDesc:'Profilini oluşturur, temsil etmek istediğin ülkeyi seçer ve maçlar başlamadan önce tahminlerini girersin. Kazandığın puanlar kişisel sıralamana ve ülke puanına yansır.',rulesMatchDesc:'Her maç için skor tahmini maç başlamadan önce yapılır. Tahmin kaydedildikten sonra değiştirilemez.',rulesTournamentDesc:'Şampiyon, finalistler, gol kralı, asist kralı ve turnuvanın oyuncusu için turnuva tahminleri yapılır. Bu tahminler bir kez kaydedilir.',championCorrect:'Şampiyon doğru',finalistCorrect:'Finalist doğru',topScorerCorrect:'Gol kralı doğru',topAssistCorrect:'Asist kralı doğru',playerTournamentCorrect:'Turnuvanın oyuncusu doğru',exactScoreCorrect:'Tam skor doğru',resultDiffCorrect:'Sonuç doğru + gol farkı doğru',resultOnlyCorrect:'Sadece sonuç doğru',wrongPrediction:'Yanlış tahmin',rulesCountryDesc:'Kullanıcının kazandığı her puan, kayıt olurken seçtiği ülkenin kolektif toplamına da katkı sağlar.',rulesSocialDesc:'Günün iyi tahminleri, hızlı yükselen kullanıcıları ve öne çıkan ülkeleri sosyal hesaplarımızda paylaşılabilir.',rulesGeneralDesc:'Tahminler maç başlamadan önce yapılmalıdır. Maç başladıktan sonra tahmin değiştirilemez. Her kullanıcı yalnızca bir hesap kullanabilir. Haksız avantaj sağlayan hesaplar kaldırılabilir.',rulesWarningDesc:'Bu platform bahis sitesi değildir. Para karşılığı maç sonucu üzerinden kazanç vaat etmez. Amaç Dünya Kupası boyunca küresel bir tahmin, rekabet ve topluluk deneyimi oluşturmaktır.',points250:'250 puan',points150:'150 puan',points100:'100 puan',points80:'80 puan',points30:'30 puan',points10:'10 puan',points0:'0 puan',emailPlaceholder:'email@ornek.com',passwordPlaceholder:'Şifre',savePrediction:'Tahmini Kaydet',predictionSaved:'Tahmin kaydedildi, değiştirilemez.',loginToPredict:'Tahmin yapmak için giriş yapmalısın.',enterBothScores:'Lütfen iki takım için de skor gir.',matchLabel:'Maç',goalSuffix:'gol',team1:'Takım 1',team2:'Takım 2',lockedTournamentMessage:'Turnuva tahminlerin kaydedildi. Bu tahminler artık değiştirilemez.',oneTournamentOnly:'Turnuva tahminlerini yalnızca bir kez yapabilirsin.',tournamentSaved:'Turnuva tahminlerin kaydedildi.',noRankingData:'Henüz sıralama verisi yok.',referralInput:'Davet Kodu',referralPlaceholder:'Varsa davet kodunu gir'},
  en:{navHome:'Home',navPredictions:'Predictions',navRankings:'Rankings',navRules:'Rules',navProfile:'Profile',navLogin:'Log In',navSignup:'Sign Up',navLogout:'Log Out',navStart:'Start Predicting',homeLabel:'World Cup 2026 · 104 Matches',homeTitle:'104 Matches.<br>48 Nations.<br>Millions of Football Fans.<br><span class="htitle-a">One World Cup Arena.</span>',homeSub:'Predict every match, collect points, support your country, and compete in the same global ranking with football fans from all around the world during World Cup 2026.',rankingsSee:'View Rankings',statMatches:'Matches',statCountries:'Nations',statDays:'Days',statCompetition:'Competition',manifestLabel:'Our Manifesto',manifest1:'The World Cup comes once every four years.',manifest2:'Millions of people from different countries watch the same matches, celebrate the same goals, and share the same heartbreaks.',manifest3:'World Cup Arena brings these people together in one shared experience.',manifest4:'This is not just a prediction game.',manifest5:'This is a global football arena where every point shapes both your destiny and the country you represent.',gameLabel:'Game',howTitle:'How It Works',how1Title:'Make Tournament Predictions',how1Desc:'Before the first whistle, predict the champion, finalists, top scorer, top assister, and player of the tournament.',how2Title:'Predict Match Scores',how2Desc:'Submit your exact score prediction before every match. The closer you are, the more points you earn.',how3Title:'Collect Points',how3Desc:'Earn points from exact scores, correct results, streaks, and tournament predictions. Every match matters.',how4Title:'Represent Your Country',how4Desc:'Your points contribute to your chosen country’s overall ranking. Carry your flag with every prediction.',communityLabel:'Community',socialTitle:'Social Experience',joinNow:'Join Now',ctaTitle:'Become part of the global<br><span class="cta-ta">World Cup experience.</span>',ctaSub:'104 matches. Countless predictions. One global arena.',predLabel:'Predictions',predTitle:'Predictions',warningLabel:'Important Notice',warningTitle:'Check your picks before saving',warning1:'Tournament predictions can only be made once. After saving, they cannot be changed.',warning2:'Choose your champion, finalists, top scorer, top assister, and player of the tournament carefully.',warning3:'Match score predictions can also be made only once for each match. Check your score before saving.',warning4:'Do not rush. Early predictions can be misleading.',tournamentPredictions:'Tournament Predictions',tournamentGeneral:'General Tournament Predictions',championQuestion:'Who will be champion?',finalist1:'Finalist 1',finalist2:'Finalist 2',topScorerQuestion:'Who will be top scorer?',topAssistQuestion:'Who will be assist leader?',playerTournamentQuestion:'Who will be player of the tournament?',saveTournament:'Save Tournament Predictions',goMatchPredictions:'Go to Match Predictions',matchPredictions:'Match Predictions',groupMatches:'Group Stage Matches',showAllMatches:'Show All Matches',rankingsLabel:'Rankings',rankingsTitle:'Rankings',individualRanking:'Individual Ranking',countryParticipation:'Country Participation',collectiveRanking:'Country / Collective Points Ranking',showMore:'Show More',showAllRanking:'Show Full Ranking',closeList:'Close List',points:'points',participant:'participants',user:'users',selfTag:'You',rulesLabel:'Rules',rulesTitle:'Rules and Scoring',whatIs:'What Is World Cup Arena?',howJoin:'How Do You Join?',rulesMatch:'Match Predictions',rulesTournament:'Tournament Predictions',pointsSystem:'Scoring System',countryPoints:'Country Points',socialShares:'Social Shares',premiumVisibility:'Premium Visibility',generalRules:'General Rules',importantWarning:'Important Notice',profileLabel:'Profile',profileTitle:'Profile',loginRequired:'Login Required',loginRequiredText:'You must log in to view your profile.',profileCard:'Profile Card',username:'Username',email:'Email',country:'Country',totalPoints:'Total Points',worldRank:'World Rank',countryRank:'Country Rank',referralCode:'Referral Code',premiumText:'Premium users can appear in rankings with their chosen name, add a profile photo, and create a shareable user card.',premiumUpgrade:'Go Premium',customUsername:'Custom username',profilePhoto:'Profile photo',visibleName:'Visible ranking name',shareableCard:'Shareable user card',xTagChance:'Chance to be tagged on X',account:'Account',password:'Password',countrySelect:'Country selection',noAccount:'No account? Sign up',hasAccount:'Already have an account? Log in',navStartIcon:'⚽ Start Predicting',rulesWhatDesc:'World Cup Arena is a prediction and community experience where you collect points through match and tournament predictions, represent your country, and compete in a global ranking throughout the World Cup.',rulesJoinDesc:'Create your profile, choose the country you want to represent, and enter your predictions before matches begin. Your points count toward your personal ranking and your country score.',rulesMatchDesc:'For each match, score predictions must be made before kickoff. Once a prediction is saved, it cannot be changed.',rulesTournamentDesc:'Tournament predictions are made for the champion, finalists, top scorer, assist leader, and player of the tournament. These predictions are saved once.',championCorrect:'Champion correct',finalistCorrect:'Finalist correct',topScorerCorrect:'Top scorer correct',topAssistCorrect:'Assist leader correct',playerTournamentCorrect:'Player of the tournament correct',exactScoreCorrect:'Exact score correct',resultDiffCorrect:'Correct result + correct goal difference',resultOnlyCorrect:'Correct result only',wrongPrediction:'Wrong prediction',rulesCountryDesc:'Every point a user earns also contributes to the collective total of the country selected during registration.',rulesSocialDesc:'The best predictions of the day, fastest-rising users, and featured countries may be shared on our social accounts.',rulesGeneralDesc:'Predictions must be made before the match begins. After kickoff, predictions cannot be changed. Each user may use only one account. Accounts that gain unfair advantage may be removed.',rulesWarningDesc:'This platform is not a betting site. It does not promise winnings based on match results in exchange for money. The goal is to create a global prediction, competition, and community experience throughout the World Cup.',points250:'250 points',points150:'150 points',points100:'100 points',points80:'80 points',points30:'30 points',points10:'10 points',points0:'0 points',emailPlaceholder:'email@example.com',passwordPlaceholder:'Password',savePrediction:'Save Prediction',predictionSaved:'Prediction saved, cannot be changed.',loginToPredict:'You must log in to make predictions.',enterBothScores:'Please enter scores for both teams.',matchLabel:'Match',goalSuffix:'goals',team1:'Team 1',team2:'Team 2',lockedTournamentMessage:'Your tournament predictions have been saved. They can no longer be changed.',oneTournamentOnly:'You can make tournament predictions only once.',tournamentSaved:'Your tournament predictions have been saved.',noRankingData:'No ranking data yet.',referralInput:'Referral Code',referralPlaceholder:'Enter referral code if you have one'},
  es:{navHome:'Inicio',navPredictions:'Predicciones',navRankings:'Ranking',navRules:'Reglas',navProfile:'Perfil',navLogin:'Iniciar Sesión',navSignup:'Registrarse',navLogout:'Cerrar Sesión',navStart:'Empezar a Predecir',homeLabel:'Mundial 2026 · 104 Partidos',homeTitle:'104 Partidos.<br>48 Países.<br>Millones de Aficionados.<br><span class="htitle-a">Una Arena Mundial.</span>',homeSub:'Predice cada partido, suma puntos, apoya a tu país y compite en el mismo ranking global con aficionados de todo el mundo durante el Mundial 2026.',rankingsSee:'Ver Ranking',statMatches:'Partidos',statCountries:'Países',statDays:'Días',statCompetition:'Competencia',manifestLabel:'Nuestro Manifiesto',manifest1:'El Mundial llega una vez cada cuatro años.',manifest2:'Millones de personas de distintos países ven los mismos partidos, celebran los mismos goles y comparten las mismas emociones.',manifest3:'World Cup Arena reúne a esas personas en una experiencia común.',manifest4:'Esto no es solo un juego de predicciones.',manifest5:'Es una arena global de fútbol donde cada punto influye en tu destino y en el del país que representas.',gameLabel:'Juego',howTitle:'Cómo Funciona',how1Title:'Haz Predicciones del Torneo',how1Desc:'Antes del primer silbato, predice el campeón, los finalistas, el máximo goleador, el líder en asistencias y el jugador del torneo.',how2Title:'Predice Marcadores',how2Desc:'Envía tu marcador exacto antes de cada partido. Cuanto más cerca estés, más puntos ganas.',how3Title:'Suma Puntos',how3Desc:'Gana puntos por marcadores exactos, resultados correctos, rachas y predicciones del torneo. Cada partido cuenta.',how4Title:'Representa a Tu País',how4Desc:'Tus puntos contribuyen al ranking general del país que elegiste. Lleva tu bandera en cada predicción.',communityLabel:'Comunidad',socialTitle:'Experiencia Social',joinNow:'Únete Ahora',ctaTitle:'Sé parte de la experiencia<br><span class="cta-ta">global del Mundial.</span>',ctaSub:'104 partidos. Innumerables predicciones. Una arena global.',predLabel:'Predicciones',predTitle:'Predicciones',warningLabel:'Aviso Importante',warningTitle:'Revisa tus elecciones antes de guardar',warning1:'Las predicciones del torneo solo se pueden hacer una vez. Después de guardarlas, no se pueden cambiar.',warning2:'Elige con cuidado tu campeón, finalistas, máximo goleador, líder de asistencias y jugador del torneo.',warning3:'Las predicciones de marcador también solo se pueden hacer una vez por partido. Revisa tu marcador antes de guardar.',warning4:'No te apresures. Las predicciones tempranas pueden ser engañosas.',tournamentPredictions:'Predicciones del Torneo',tournamentGeneral:'Predicciones Generales del Torneo',championQuestion:'¿Quién será campeón?',finalist1:'Finalista 1',finalist2:'Finalista 2',topScorerQuestion:'¿Quién será el máximo goleador?',topAssistQuestion:'¿Quién será el líder de asistencias?',playerTournamentQuestion:'¿Quién será el jugador del torneo?',saveTournament:'Guardar Predicciones del Torneo',goMatchPredictions:'Ir a Predicciones de Partidos',matchPredictions:'Predicciones de Partidos',groupMatches:'Partidos de Fase de Grupos',showAllMatches:'Mostrar Todos los Partidos',rankingsLabel:'Ranking',rankingsTitle:'Ranking',individualRanking:'Ranking Individual',countryParticipation:'Participación por País',collectiveRanking:'Ranking de Países / Puntos Colectivos',showMore:'Ver Más',showAllRanking:'Mostrar Ranking Completo',closeList:'Cerrar Lista',points:'puntos',participant:'participantes',user:'usuarios',selfTag:'Tú',rulesLabel:'Reglas',rulesTitle:'Reglas y Puntuación',whatIs:'¿Qué es World Cup Arena?',howJoin:'¿Cómo Participas?',rulesMatch:'Predicciones de Partidos',rulesTournament:'Predicciones del Torneo',pointsSystem:'Sistema de Puntos',countryPoints:'Puntos del País',socialShares:'Publicaciones Sociales',premiumVisibility:'Visibilidad Premium',generalRules:'Reglas Generales',importantWarning:'Aviso Importante',profileLabel:'Perfil',profileTitle:'Perfil',loginRequired:'Inicio de Sesión Requerido',loginRequiredText:'Debes iniciar sesión para ver tu perfil.',profileCard:'Tarjeta de Perfil',username:'Usuario',email:'Correo',country:'País',totalPoints:'Puntos Totales',worldRank:'Ranking Mundial',countryRank:'Ranking Nacional',referralCode:'Código de Invitación',premiumText:'Los usuarios Premium pueden aparecer en el ranking con el nombre que elijan, añadir foto de perfil y crear una tarjeta compartible.',premiumUpgrade:'Pasar a Premium',customUsername:'Nombre personalizado',profilePhoto:'Foto de perfil',visibleName:'Nombre visible en ranking',shareableCard:'Tarjeta compartible',xTagChance:'Oportunidad de etiqueta en X',account:'Cuenta',password:'Contraseña',countrySelect:'Selección de país',noAccount:'¿No tienes cuenta? Regístrate',hasAccount:'¿Ya tienes cuenta? Inicia sesión',navStartIcon:'⚽ Empezar a Predecir',rulesWhatDesc:'World Cup Arena es una experiencia de predicción y comunidad donde sumas puntos con predicciones de partidos y torneo, representas a tu país y compites en un ranking global durante el Mundial.',rulesJoinDesc:'Crea tu perfil, elige el país que quieres representar e ingresa tus predicciones antes de que empiecen los partidos. Tus puntos cuentan para tu ranking personal y el puntaje de tu país.',rulesMatchDesc:'Para cada partido, la predicción del marcador debe hacerse antes del inicio. Una vez guardada, no se puede cambiar.',rulesTournamentDesc:'Las predicciones del torneo incluyen campeón, finalistas, máximo goleador, líder de asistencias y jugador del torneo. Estas predicciones se guardan una sola vez.',championCorrect:'Campeón correcto',finalistCorrect:'Finalista correcto',topScorerCorrect:'Máximo goleador correcto',topAssistCorrect:'Líder de asistencias correcto',playerTournamentCorrect:'Jugador del torneo correcto',exactScoreCorrect:'Marcador exacto correcto',resultDiffCorrect:'Resultado correcto + diferencia de goles correcta',resultOnlyCorrect:'Solo resultado correcto',wrongPrediction:'Predicción incorrecta',rulesCountryDesc:'Cada punto que gana un usuario también contribuye al total colectivo del país elegido durante el registro.',rulesSocialDesc:'Las mejores predicciones del día, los usuarios que más suben y los países destacados pueden compartirse en nuestras redes sociales.',rulesGeneralDesc:'Las predicciones deben hacerse antes de que comience el partido. Después del inicio, no se pueden cambiar. Cada usuario puede usar solo una cuenta. Las cuentas con ventaja injusta pueden ser eliminadas.',rulesWarningDesc:'Esta plataforma no es un sitio de apuestas. No promete ganancias por resultados de partidos a cambio de dinero. El objetivo es crear una experiencia global de predicción, competencia y comunidad durante el Mundial.',points250:'250 puntos',points150:'150 puntos',points100:'100 puntos',points80:'80 puntos',points30:'30 puntos',points10:'10 puntos',points0:'0 puntos',emailPlaceholder:'email@ejemplo.com',passwordPlaceholder:'Contraseña',savePrediction:'Guardar Pronóstico',predictionSaved:'Pronóstico guardado, no se puede cambiar.',loginToPredict:'Debes iniciar sesión para hacer pronósticos.',enterBothScores:'Ingresa el marcador de ambos equipos.',matchLabel:'Partido',goalSuffix:'goles',team1:'Equipo 1',team2:'Equipo 2',lockedTournamentMessage:'Tus predicciones del torneo se han guardado. Ya no se pueden cambiar.',oneTournamentOnly:'Solo puedes hacer predicciones del torneo una vez.',tournamentSaved:'Tus predicciones del torneo se han guardado.',noRankingData:'Aún no hay datos de ranking.',referralInput:'Código de Invitación',referralPlaceholder:'Ingresa tu código si tienes uno'}
};
const countryTranslations={
  'Mexico':{flag:'🇲🇽',tr:'Meksika',en:'Mexico',es:'México',aliases:['Meksika','México']},
  'South Africa':{flag:'🇿🇦',tr:'Güney Afrika',en:'South Africa',es:'Sudáfrica',aliases:['Güney Afrika','Sudáfrica']},
  'South Korea':{flag:'🇰🇷',tr:'Güney Kore',en:'South Korea',es:'Corea del Sur',aliases:['Güney Kore','Corea del Sur']},
  'Czechia':{flag:'🇨🇿',tr:'Çekya',en:'Czechia',es:'Chequia',aliases:['Çekya','Chequia']},
  'Canada':{flag:'🇨🇦',tr:'Kanada',en:'Canada',es:'Canadá',aliases:['Kanada','Canadá']},
  'Bosnia and Herzegovina':{flag:'🇧🇦',tr:'Bosna Hersek',en:'Bosnia and Herzegovina',es:'Bosnia y Herzegovina',aliases:['Bosna Hersek','Bosnia y Herzegovina']},
  'Qatar':{flag:'🇶🇦',tr:'Katar',en:'Qatar',es:'Catar',aliases:['Katar','Catar']},
  'Switzerland':{flag:'🇨🇭',tr:'İsviçre',en:'Switzerland',es:'Suiza',aliases:['İsviçre','Isvicre','Suiza']},
  'Brazil':{flag:'🇧🇷',tr:'Brezilya',en:'Brazil',es:'Brasil',aliases:['Brezilya','Brasil']},
  'Morocco':{flag:'🇲🇦',tr:'Fas',en:'Morocco',es:'Marruecos',aliases:['Fas','Marruecos']},
  'Haiti':{flag:'🇭🇹',tr:'Haiti',en:'Haiti',es:'Haití',aliases:['Haití']},
  'Scotland':{flag:'🏴',tr:'İskoçya',en:'Scotland',es:'Escocia',aliases:['İskoçya','Iskocya','Escocia']},
  'United States':{flag:'🇺🇸',tr:'Amerika Birleşik Devletleri',en:'United States',es:'Estados Unidos',aliases:['ABD','Amerika Birleşik Devletleri','Estados Unidos','USA','United States of America']},
  'Paraguay':{flag:'🇵🇾',tr:'Paraguay',en:'Paraguay',es:'Paraguay',aliases:['Paraguay']},
  'Australia':{flag:'🇦🇺',tr:'Avustralya',en:'Australia',es:'Australia',aliases:['Avustralya']},
  'Türkiye':{flag:'🇹🇷',tr:'Türkiye',en:'Turkey',es:'Turquía',aliases:['Turkey','Turquía','Turkiye']},
  'Germany':{flag:'🇩🇪',tr:'Almanya',en:'Germany',es:'Alemania',aliases:['Almanya','Alemania']},
  'Curaçao':{flag:'🇨🇼',tr:'Curaçao',en:'Curaçao',es:'Curazao',aliases:['Curacao','Curazao']},
  'Côte d’Ivoire':{flag:'🇨🇮',tr:'Fildişi Sahili',en:'Côte d’Ivoire',es:'Costa de Marfil',aliases:['Fildişi Sahili','Côte d’Ivoire','Cote d’Ivoire','Costa de Marfil']},
  'Ecuador':{flag:'🇪🇨',tr:'Ekvador',en:'Ecuador',es:'Ecuador',aliases:['Ekvador']},
  'Netherlands':{flag:'🇳🇱',tr:'Hollanda',en:'Netherlands',es:'Países Bajos',aliases:['Hollanda','Países Bajos']},
  'Japan':{flag:'🇯🇵',tr:'Japonya',en:'Japan',es:'Japón',aliases:['Japonya','Japón']},
  'Sweden':{flag:'🇸🇪',tr:'İsveç',en:'Sweden',es:'Suecia',aliases:['İsveç','Isvec','Suecia']},
  'Tunisia':{flag:'🇹🇳',tr:'Tunus',en:'Tunisia',es:'Túnez',aliases:['Tunus','Túnez']},
  'Spain':{flag:'🇪🇸',tr:'İspanya',en:'Spain',es:'España',aliases:['İspanya','Ispanya','España']},
  'Cabo Verde':{flag:'🇨🇻',tr:'Cabo Verde',en:'Cabo Verde',es:'Cabo Verde',aliases:['Cape Verde','Yeşil Burun Adaları']},
  'Saudi Arabia':{flag:'🇸🇦',tr:'Suudi Arabistan',en:'Saudi Arabia',es:'Arabia Saudita',aliases:['Suudi Arabistan','Arabia Saudita']},
  'Uruguay':{flag:'🇺🇾',tr:'Uruguay',en:'Uruguay',es:'Uruguay',aliases:['Uruguay']},
  'Belgium':{flag:'🇧🇪',tr:'Belçika',en:'Belgium',es:'Bélgica',aliases:['Belçika','Belcika','Bélgica']},
  'Egypt':{flag:'🇪🇬',tr:'Mısır',en:'Egypt',es:'Egipto',aliases:['Mısır','Misir','Egipto']},
  'Iran':{flag:'🇮🇷',tr:'İran',en:'Iran',es:'Irán',aliases:['İran','Irán']},
  'New Zealand':{flag:'🇳🇿',tr:'Yeni Zelanda',en:'New Zealand',es:'Nueva Zelanda',aliases:['Yeni Zelanda','Nueva Zelanda']},
  'France':{flag:'🇫🇷',tr:'Fransa',en:'France',es:'Francia',aliases:['Fransa','Francia']},
  'Senegal':{flag:'🇸🇳',tr:'Senegal',en:'Senegal',es:'Senegal',aliases:['Senegal']},
  'Iraq':{flag:'🇮🇶',tr:'Irak',en:'Iraq',es:'Irak',aliases:['Irak']},
  'Norway':{flag:'🇳🇴',tr:'Norveç',en:'Norway',es:'Noruega',aliases:['Norveç','Norvec','Noruega']},
  'Argentina':{flag:'🇦🇷',tr:'Arjantin',en:'Argentina',es:'Argentina',aliases:['Arjantin']},
  'Algeria':{flag:'🇩🇿',tr:'Cezayir',en:'Algeria',es:'Argelia',aliases:['Cezayir','Argelia']},
  'Austria':{flag:'🇦🇹',tr:'Avusturya',en:'Austria',es:'Austria',aliases:['Avusturya']},
  'Jordan':{flag:'🇯🇴',tr:'Ürdün',en:'Jordan',es:'Jordania',aliases:['Ürdün','Urdun','Jordania']},
  'Portugal':{flag:'🇵🇹',tr:'Portekiz',en:'Portugal',es:'Portugal',aliases:['Portekiz']},
  'DRC':{flag:'🇨🇩',tr:'Kongo DC',en:'DRC',es:'RD Congo',aliases:['Kongo DC','Democratic Republic of the Congo','RD Congo']},
  'Uzbekistan':{flag:'🇺🇿',tr:'Özbekistan',en:'Uzbekistan',es:'Uzbekistán',aliases:['Özbekistan','Ozbekistan','Uzbekistán']},
  'Colombia':{flag:'🇨🇴',tr:'Kolombiya',en:'Colombia',es:'Colombia',aliases:['Kolombiya']},
  'England':{flag:'🏴',tr:'İngiltere',en:'England',es:'Inglaterra',aliases:['İngiltere','Ingiltere','Inglaterra']},
  'Croatia':{flag:'🇭🇷',tr:'Hırvatistan',en:'Croatia',es:'Croacia',aliases:['Hırvatistan','Hirvatistan','Croacia']},
  'Ghana':{flag:'🇬🇭',tr:'Gana',en:'Ghana',es:'Ghana',aliases:['Gana']},
  'Panama':{flag:'🇵🇦',tr:'Panama',en:'Panama',es:'Panamá',aliases:['Panamá']}
};
function normalizeCountryName(value){return String(value || '').trim().toLocaleLowerCase('tr-TR');}
function findCountryEntry(value){
  const normalized=normalizeCountryName(value);
  return Object.entries(countryTranslations).find(([key,item])=>normalizeCountryName(key)===normalized || [item.tr,item.en,item.es,...(item.aliases || [])].some(alias=>normalizeCountryName(alias)===normalized));
}
function canonicalCountry(country){
  const entry=findCountryEntry(country);
  return entry ? entry[0] : (country || 'Unknown');
}
function formatCountry(country,lang=currentLanguage){
  if(country===null || country===undefined || String(country).trim()==='') return 'Unknown';
  const entry=findCountryEntry(country);
  if(!entry) return country || 'Unknown';
  const item=entry[1];
  return `${item.flag} ${item[lang] || item.en || item.tr || 'Unknown'}`;
}
function formatDisplayName(name){return String(name || 'Player').replace(/^Oyuncu#/,'Player#');}
function updateCountryOptions(){
  ['champion','finalist_1','finalist_2','signupCountry'].forEach(id=>{
    const select=document.getElementById(id);
    if(!select) return;
    [...select.options].forEach(option=>{
      if(!option.dataset.countryValue){option.dataset.countryValue=option.value || option.textContent;option.value=option.dataset.countryValue;}
      if(findCountryEntry(option.dataset.countryValue)){option.textContent=formatCountry(option.dataset.countryValue);}
    });
  });
}
let currentLanguage=localStorage.getItem('selectedLanguage') || 'tr';
function translate(key){return translations[currentLanguage]?.[key] || translations.tr[key] || key;}
function applyLanguage(){
  document.documentElement.lang=currentLanguage;
  document.querySelectorAll('[data-i18n]').forEach(el=>{el.textContent=translate(el.dataset.i18n);});
  document.querySelectorAll('[data-i18n-html]').forEach(el=>{el.innerHTML=translate(el.dataset.i18nHtml);});
  document.querySelectorAll('[data-i18n-placeholder]').forEach(el=>{el.setAttribute('placeholder',translate(el.dataset.i18nPlaceholder));});
  document.querySelectorAll('[data-lang]').forEach(btn=>btn.classList.toggle('active',btn.dataset.lang===currentLanguage));
  updateCountryOptions();
}
function setLanguage(lang){
  if(!translations[lang]) return;
  currentLanguage=lang;
  localStorage.setItem('selectedLanguage',lang);
  applyLanguage();
  createOrLoadProfile();
  loadMatches();
  loadRankings();
}
const hdr=document.getElementById('hdr'),hbg=document.getElementById('hbg'),mob=document.getElementById('mobnav');
const showAllMatchesBtn=document.getElementById('showAllMatches');

function showAuthMessage(message){
  const box=document.getElementById('authMessage');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function formatAuthError(error){
  const message=error?.message || String(error || 'Bir hata oluştu.');
  const lower=message.toLowerCase();
  if(lower.includes('email not confirmed')){
    return 'E-posta doğrulaması gerekiyor. Supabase ayarlarından email confirmation kapatılabilir.';
  }
  if(lower.includes('rate') || lower.includes('security') || lower.includes('too many') || lower.includes('after')){
    return 'Çok kısa sürede tekrar denedin. Lütfen biraz bekleyip yeniden dene.';
  }
  return message;
}

function getSupabaseClient(){
  if(!window.supabaseClient){
    showAuthMessage('Supabase bağlantısı için js/supabase.js içindeki SUPABASE_URL ve SUPABASE_ANON_KEY değerlerini doldur.');
    return null;
  }
  return window.supabaseClient;
}

function generatePlayerName(){
  return 'Player#'+Math.floor(100000+Math.random()*900000);
}

function generateReferralCode(){
  return 'WCA'+Math.floor(100000+Math.random()*900000);
}

function showAuthTab(tab){
  const loginForm=document.getElementById('loginForm');
  const signupForm=document.getElementById('signupForm');
  const loginTab=document.getElementById('loginTab');
  const signupTab=document.getElementById('signupTab');
  if(!loginForm || !signupForm) return;
  const isSignup=tab==='signup';
  loginForm.hidden=isSignup;
  signupForm.hidden=!isSignup;
  loginForm.style.display=isSignup?'none':'flex';
  signupForm.style.display=isSignup?'flex':'none';
  loginTab?.setAttribute('aria-selected',String(!isSignup));
  signupTab?.setAttribute('aria-selected',String(isSignup));
  loginTab?.classList.toggle('btn-p',!isSignup);
  loginTab?.classList.toggle('btn-o',isSignup);
  signupTab?.classList.toggle('btn-p',isSignup);
  signupTab?.classList.toggle('btn-o',!isSignup);
}

function openAuthModal(tab='login'){
  const modal=document.getElementById('authModal');
  if(!modal){window.location.href='index.html#'+tab;return;}
  showAuthMessage('');
  showAuthTab(tab);
  modal.hidden=false;
  document.body.classList.add('modal-open');
}

function closeAuthModal(){
  const modal=document.getElementById('authModal');
  if(!modal) return;
  modal.hidden=true;
  document.body.classList.remove('modal-open');
}

async function getCurrentUser(){
  const client=getSupabaseClient();
  if(!client) return null;
  const {data,error}=await client.auth.getUser();
  if(error) return null;
  return data.user || null;
}

function handlePredictionStart(event){
  event?.preventDefault();
  getCurrentUser().then(user=>{
    if(user){window.location.href='predictions.html';return;}
    openAuthModal('login');
  });
  return false;
}

async function loadProfileForUser(user){
  const client=getSupabaseClient();
  if(!client || !user) return null;
  let {data:profile,error}=await client.from('profiles').select('*').eq('id',user.id).single();
  if(error || !profile){
    const country=user.user_metadata?.country || 'Türkiye';
    const insertProfile={
      id:user.id,
      email:user.email,
      display_name:generatePlayerName(),
      country,
      premium:false,
      total_points:0,
      referral_code:generateReferralCode(),
      referred_by:null,
      created_at:new Date().toISOString()
    };
    const {data:newProfile,error:insertError}=await client.from('profiles').insert(insertProfile).select('*').single();
    if(insertError){showAuthMessage(insertError.message);return null;}
    profile=newProfile;
  }
  return profile;
}

function fillProfileFields(profile,user){
  document.getElementById('profileDisplayName') && (document.getElementById('profileDisplayName').textContent=formatDisplayName(profile.display_name));
  document.getElementById('profileName') && (document.getElementById('profileName').textContent=formatDisplayName(profile.display_name));
  document.getElementById('profileEmail') && (document.getElementById('profileEmail').textContent=user.email || profile.email || '-');
  document.getElementById('profileCountry') && (document.getElementById('profileCountry').textContent=formatCountry(profile.country));
  document.getElementById('profileCountryTop') && (document.getElementById('profileCountryTop').textContent=formatCountry(profile.country));
  document.getElementById('profilePoints') && (document.getElementById('profilePoints').textContent=profile.total_points ?? 0);
  document.getElementById('profileReferral') && (document.getElementById('profileReferral').textContent=profile.referral_code || '-');
}

function showTournamentPredictionMessage(message){
  const box=document.getElementById('tournamentPredictionMessage');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function setSelectValue(id,value){
  const select=document.getElementById(id);
  if(!select || !value) return;
  const option=[...select.options].find(item=>item.value===value || item.textContent===value);
  if(option) select.value=option.value;
}

function setTournamentPredictionLocked(isLocked){
  ['champion','finalist_1','finalist_2','top_scorer','top_assist','player_of_tournament'].forEach(id=>{
    const field=document.getElementById(id);
    if(field) field.disabled=isLocked;
  });
  const saveButton=document.getElementById('saveTournamentPredictions');
  if(saveButton){
    saveButton.disabled=isLocked;
    saveButton.hidden=isLocked;
  }
}

async function loadTournamentPredictions(){
  const saveButton=document.getElementById('saveTournamentPredictions');
  if(!saveButton) return;
  setTournamentPredictionLocked(false);
  const client=getSupabaseClient();
  if(!client) return;
  const user=await getCurrentUser();
  if(!user) return;
  const {data,error}=await client.from('tournament_predictions').select('champion,finalist_1,finalist_2,top_scorer,top_assist,player_of_tournament').eq('user_id',user.id).maybeSingle();
  if(error){showTournamentPredictionMessage(error.message);return;}
  if(!data) return;
  setSelectValue('champion',data.champion);
  setSelectValue('finalist_1',data.finalist_1);
  setSelectValue('finalist_2',data.finalist_2);
  setSelectValue('top_scorer',data.top_scorer);
  setSelectValue('top_assist',data.top_assist);
  setSelectValue('player_of_tournament',data.player_of_tournament);
  setTournamentPredictionLocked(true);
  showTournamentPredictionMessage(translate('lockedTournamentMessage'));
}

async function saveTournamentPredictions(){
  const client=getSupabaseClient();
  if(!client) return;
  const user=await getCurrentUser();
  if(!user){
    showTournamentPredictionMessage(translate('loginToPredict'));
    setTimeout(()=>openAuthModal('login'),700);
    return;
  }

  const {data:existing,error:existingError}=await client.from('tournament_predictions').select('user_id').eq('user_id',user.id).maybeSingle();
  if(existingError){showTournamentPredictionMessage(existingError.message);return;}
  if(existing){
    setTournamentPredictionLocked(true);
    showTournamentPredictionMessage(translate('oneTournamentOnly'));
    return;
  }

  const payload={
    user_id:user.id,
    champion:document.getElementById('champion')?.value,
    finalist_1:document.getElementById('finalist_1')?.value,
    finalist_2:document.getElementById('finalist_2')?.value,
    top_scorer:document.getElementById('top_scorer')?.value,
    top_assist:document.getElementById('top_assist')?.value,
    player_of_tournament:document.getElementById('player_of_tournament')?.value
  };

  const {error}=await client.from('tournament_predictions').insert(payload);
  if(error){
    const lower=(error.message || '').toLowerCase();
    if(lower.includes('duplicate') || lower.includes('unique')){
      setTournamentPredictionLocked(true);
      showTournamentPredictionMessage(translate('oneTournamentOnly'));
      return;
    }
    showTournamentPredictionMessage(error.message);
    return;
  }
  setTournamentPredictionLocked(true);
  showTournamentPredictionMessage(translate('lockedTournamentMessage'));
}
function showMatchPredictionMessage(message){
  const box=document.getElementById('matchPredictionMessage');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function lockMatchCard(card,homeScore,awayScore,message=translate('predictionSaved')){
  const homeInput=card.querySelector('[data-role="home-score"]');
  const awayInput=card.querySelector('[data-role="away-score"]');
  const saveButton=card.querySelector('[data-role="save-match"]');
  const messageBox=card.querySelector('[data-role="match-message"]');
  if(homeInput){homeInput.value=homeScore;homeInput.disabled=true;}
  if(awayInput){awayInput.value=awayScore;awayInput.disabled=true;}
  if(saveButton){saveButton.disabled=true;saveButton.hidden=true;}
  if(messageBox){messageBox.textContent=message;messageBox.hidden=false;}
}

function createMatchCard(match,index,prediction){
  const card=document.createElement('div');
  card.className='rank-panel'+(index>=12?' extra-match':'');
  card.style.padding='18px';
  if(index>=12) card.hidden=true;
  card.dataset.matchId=match.id;

  const meta=document.createElement('p');
  meta.className='slabel';
  meta.textContent=translate('matchLabel')+' '+match.match_no+' · '+(match.match_date || '-')+' · '+(match.match_time || '-');
  card.appendChild(meta);

  const group=document.createElement('h3');
  group.className='hc-t';
  group.textContent=match.group_name || '-';
  card.appendChild(group);

  const teams=document.createElement('div');
  teams.className='rrow';
  teams.style.padding='12px 0';
  const home=document.createElement('span');
  home.className='rname';
  home.textContent=match.team_home || '-';
  const vs=document.createElement('span');
  vs.className='rval';
  vs.textContent='vs';
  const away=document.createElement('span');
  away.className='rname';
  away.style.textAlign='right';
  away.textContent=match.team_away || '-';
  teams.append(home,vs,away);
  card.appendChild(teams);

  const inputs=document.createElement('div');
  inputs.style.display='flex';
  inputs.style.gap='10px';
  inputs.style.marginTop='12px';
  const homeInput=document.createElement('input');
  homeInput.type='number';
  homeInput.min='0';
  homeInput.placeholder=(match.team_home || translate('team1'))+' '+translate('goalSuffix');
  homeInput.dataset.role='home-score';
  homeInput.style.cssText='width:100%;padding:12px;border-radius:8px;border:1px solid var(--border);background:var(--bg-raised);color:var(--t1)';
  const awayInput=document.createElement('input');
  awayInput.type='number';
  awayInput.min='0';
  awayInput.placeholder=(match.team_away || translate('team2'))+' '+translate('goalSuffix');
  awayInput.dataset.role='away-score';
  awayInput.style.cssText='width:100%;padding:12px;border-radius:8px;border:1px solid var(--border);background:var(--bg-raised);color:var(--t1)';
  inputs.append(homeInput,awayInput);
  card.appendChild(inputs);

  const message=document.createElement('div');
  message.className='hc-d';
  message.dataset.role='match-message';
  message.style.marginTop='12px';
  message.hidden=true;
  card.appendChild(message);

  const button=document.createElement('button');
  button.className='btn btn-o';
  button.type='button';
  button.dataset.role='save-match';
  button.style.marginTop='14px';
  button.textContent=translate('savePrediction');
  button.dataset.i18n='savePrediction';
  button.addEventListener('click',()=>saveMatchPrediction(match.id,card));
  card.appendChild(button);

  if(prediction){
    lockMatchCard(card,prediction.predicted_home_score,prediction.predicted_away_score);
  }
  return card;
}

async function loadMatches(){
  const list=document.getElementById('matchesList');
  if(!list) return;
  const client=getSupabaseClient();
  if(!client) return;
  showMatchPredictionMessage('');
  const {data:matches,error}=await client.from('matches').select('id,match_no,match_date,match_time,group_name,team_home,team_away,status').order('match_no',{ascending:true});
  if(error){showMatchPredictionMessage(error.message);return;}

  const user=await getCurrentUser();
  let predictionMap=new Map();
  if(user){
    const {data:predictions,error:predictionError}=await client.from('match_predictions').select('match_id,predicted_home_score,predicted_away_score').eq('user_id',user.id);
    if(predictionError){showMatchPredictionMessage(predictionError.message);return;}
    predictionMap=new Map((predictions || []).map(item=>[item.match_id,item]));
  }

  list.innerHTML='';
  (matches || []).forEach((match,index)=>{
    list.appendChild(createMatchCard(match,index,predictionMap.get(match.id)));
  });

  const showAllButton=document.getElementById('showAllMatches');
  if(showAllButton){
    showAllButton.hidden=(matches || []).length<=12;
  }
}

async function saveMatchPrediction(matchId,card){
  const client=getSupabaseClient();
  if(!client) return;
  const user=await getCurrentUser();
  const messageBox=card.querySelector('[data-role="match-message"]');
  if(!user){
    if(messageBox){messageBox.textContent=translate('loginToPredict');messageBox.hidden=false;}
    showMatchPredictionMessage(translate('loginToPredict'));
    return;
  }

  const homeInput=card.querySelector('[data-role="home-score"]');
  const awayInput=card.querySelector('[data-role="away-score"]');
  const homeScore=homeInput?.value;
  const awayScore=awayInput?.value;
  if(homeScore==='' || awayScore===''){
    if(messageBox){messageBox.textContent=translate('enterBothScores');messageBox.hidden=false;}
    return;
  }

  const payload={
    user_id:user.id,
    match_id:matchId,
    predicted_home_score:Number(homeScore),
    predicted_away_score:Number(awayScore)
  };
  const {error}=await client.from('match_predictions').insert(payload);
  if(error){
    const lower=(error.message || '').toLowerCase();
    if(lower.includes('duplicate') || lower.includes('unique')){
      lockMatchCard(card,homeScore,awayScore);
      return;
    }
    if(messageBox){messageBox.textContent=error.message;messageBox.hidden=false;}
    return;
  }
  lockMatchCard(card,homeScore,awayScore);
}
const ADMIN_EMAIL='ican.yslyrt@gmail.com';
const WORLD_CUP_COUNTRIES=['Mexico','South Africa','South Korea','Czechia','Canada','Bosnia and Herzegovina','Qatar','Switzerland','Brazil','Morocco','Haiti','Scotland','United States','Paraguay','Australia','Türkiye','Germany','Curaçao','Côte d’Ivoire','Ecuador','Netherlands','Japan','Sweden','Tunisia','Spain','Cabo Verde','Saudi Arabia','Uruguay','Belgium','Egypt','Iran','New Zealand','France','Senegal','Iraq','Norway','Argentina','Algeria','Austria','Jordan','Portugal','DRC','Uzbekistan','Colombia','England','Croatia','Ghana','Panama'];

function showAdminMessage(message){
  const box=document.getElementById('adminAccessMessage');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function writeAdminDebug(message){
  const box=document.getElementById('adminDebug');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function clearAdminDebug(){
  const box=document.getElementById('adminDebug');
  if(box){box.textContent='';box.hidden=true;}
}

function normalizeEmail(email){
  return (email || '').trim().toLowerCase();
}

function setAdminView(isAdmin){
  const loginPanel=document.getElementById('adminLoginPanel');
  const adminPanel=document.getElementById('adminPanel');
  if(loginPanel) loginPanel.hidden=isAdmin;
  if(adminPanel) adminPanel.hidden=!isAdmin;
}

function showAdminDenied(email){
  setAdminView(false);
  showAdminMessage('Bu sayfaya erişim yetkin yok. Giriş yapılan e-posta: '+email);
}

async function isAdminSession(){
  const user=await getCurrentUser();
  if(!user) return false;
  return normalizeEmail(user.email)===ADMIN_EMAIL;
}

function createAdminMatchCard(match){
  const card=document.createElement('div');
  card.className='rank-panel feat';
  card.style.padding='18px';
  card.dataset.matchId=match.id;

  const meta=document.createElement('p');
  meta.className='slabel';
  meta.textContent=translate('matchLabel')+' '+match.match_no+' · '+(match.match_date || '-')+' · '+(match.match_time || '-');
  card.appendChild(meta);

  const group=document.createElement('h3');
  group.className='hc-t';
  group.textContent=match.group_name || '-';
  card.appendChild(group);

  const teams=document.createElement('div');
  teams.className='rrow';
  teams.style.padding='12px 0';
  const home=document.createElement('span');
  home.className='rname';
  home.textContent=match.team_home || '-';
  const vs=document.createElement('span');
  vs.className='rval';
  vs.textContent='vs';
  const away=document.createElement('span');
  away.className='rname';
  away.style.textAlign='right';
  away.textContent=match.team_away || '-';
  teams.append(home,vs,away);
  card.appendChild(teams);

  const status=document.createElement('div');
  status.className='hc-d';
  status.dataset.role='admin-status';
  status.style.marginTop='10px';
  status.textContent='Durum: '+(match.status || '-');
  card.appendChild(status);

  const inputs=document.createElement('div');
  inputs.style.display='flex';
  inputs.style.gap='10px';
  inputs.style.marginTop='14px';
  const homeInput=document.createElement('input');
  homeInput.type='number';
  homeInput.min='0';
  homeInput.placeholder=(match.team_home || 'Ev sahibi')+' skor';
  homeInput.value=match.home_score ?? '';
  homeInput.dataset.role='admin-home-score';
  homeInput.style.cssText='width:100%;padding:12px;border-radius:8px;border:1px solid var(--border);background:var(--bg-raised);color:var(--t1)';
  const awayInput=document.createElement('input');
  awayInput.type='number';
  awayInput.min='0';
  awayInput.placeholder=(match.team_away || 'Deplasman')+' skor';
  awayInput.value=match.away_score ?? '';
  awayInput.dataset.role='admin-away-score';
  awayInput.style.cssText='width:100%;padding:12px;border-radius:8px;border:1px solid var(--border);background:var(--bg-raised);color:var(--t1)';
  inputs.append(homeInput,awayInput);
  card.appendChild(inputs);

  const message=document.createElement('div');
  message.className='hc-d';
  message.dataset.role='admin-match-message';
  message.style.marginTop='12px';
  message.hidden=true;
  card.appendChild(message);

  const button=document.createElement('button');
  button.className='btn btn-p';
  button.type='button';
  button.style.marginTop='14px';
  button.dataset.role='admin-save-result';
  button.textContent=match.status==='finished'?'Sonucu Güncelle':'Sonucu Kaydet';
  button.addEventListener('click',()=>saveMatchResult(match.id));
  card.appendChild(button);
  return card;
}

async function renderAdminPanel(){
  const list=document.getElementById('adminMatchesList');
  if(!list) return;
  const client=getSupabaseClient();
  if(!client) return;
  setAdminView(true);
  showAdminMessage('');
  const {data:matches,error}=await client.from('matches').select('id,match_no,match_date,match_time,group_name,team_home,team_away,status,home_score,away_score').order('match_no',{ascending:true});
  if(error){showAdminMessage(error.message);return;}
  list.innerHTML='';
  (matches || []).forEach(match=>list.appendChild(createAdminMatchCard(match)));
}

async function loadAdminPanel(){
  const list=document.getElementById('adminMatchesList');
  if(!list) return;
  setAdminView(false);
  clearAdminDebug();
  const client=getSupabaseClient();
  if(!client) return;
  const {data,error}=await client.auth.getSession();
  if(error){showAdminMessage(error.message);return;}
  const user=data.session?.user || null;
  if(!user){showAdminMessage('');return;}
  const email=normalizeEmail(user.email);
  if(email!==ADMIN_EMAIL){showAdminDenied(email);return;}
  await renderAdminPanel();
}

async function adminLogin(){
  const client=getSupabaseClient();
  if(!client) return;
  clearAdminDebug();
  const email=document.getElementById('adminEmail')?.value.trim();
  const password=document.getElementById('adminPassword')?.value;
  const {data,error}=await client.auth.signInWithPassword({email,password});
  if(error){showAdminMessage(error.message);return;}
  const signedEmail=normalizeEmail(data.user?.email);
  if(signedEmail!==ADMIN_EMAIL){showAdminDenied(signedEmail);return;}
  applyLanguage();
  await updateHeaderAuthState();
  await renderAdminPanel();
}

function calculateMatchPoints(actualHome,actualAway,predictedHome,predictedAway){
  if(actualHome===predictedHome && actualAway===predictedAway) return 80;
  const actualDiff=actualHome-actualAway;
  const predictedDiff=predictedHome-predictedAway;
  const actualResult=Math.sign(actualDiff);
  const predictedResult=Math.sign(predictedDiff);
  if(actualResult===predictedResult && actualDiff===predictedDiff) return 30;
  if(actualResult===predictedResult) return 10;
  return 0;
}

async function calculatePointsForMatch(matchId,homeScore,awayScore){
  const client=getSupabaseClient();
  if(!client) return {processedCount:0,totalDistributed:0,hasPredictions:false};
  const {data:predictions,error}=await client
    .from('match_predictions')
    .select('id,user_id,predicted_home_score,predicted_away_score,calculated')
    .eq('match_id',matchId);

  if(error) return {processedCount:0,totalDistributed:0,hasPredictions:false,error:error.message};
  if(!predictions || predictions.length===0) return {processedCount:0,totalDistributed:0,hasPredictions:false};

  const pendingPredictions=predictions.filter(prediction=>prediction.calculated!==true);
  if(pendingPredictions.length===0) return {processedCount:0,totalDistributed:0,hasPredictions:true};

  let processedCount=0;
  let totalDistributed=0;
  for(const prediction of pendingPredictions){
    const points=calculateMatchPoints(homeScore,awayScore,Number(prediction.predicted_home_score),Number(prediction.predicted_away_score));

    const {error:updatePredictionError}=await client
      .from('match_predictions')
      .update({points,calculated:true})
      .eq('id',prediction.id)
      .eq('calculated',false);
    if(updatePredictionError) return {processedCount,totalDistributed,hasPredictions:true,error:updatePredictionError.message};

    const {data:profiles,error:profileError}=await client
      .from('profiles')
      .select('total_points')
      .eq('id',prediction.user_id)
      .limit(1);
    if(profileError) return {processedCount,totalDistributed,hasPredictions:true,error:profileError.message};

    const currentPoints=Number(profiles?.[0]?.total_points || 0);
    const {error:updateProfileError}=await client
      .from('profiles')
      .update({total_points:currentPoints+points})
      .eq('id',prediction.user_id);
    if(updateProfileError) return {processedCount,totalDistributed,hasPredictions:true,error:updateProfileError.message};

    processedCount++;
    totalDistributed+=points;
  }

  return {processedCount,totalDistributed,hasPredictions:true};
}

async function loadAdminMatches(){
  await renderAdminPanel();
}

async function saveMatchResult(matchId){
  const client=getSupabaseClient();
  if(!client) return;
  const user=await getCurrentUser();
  if(!user){setAdminView(false);showAdminMessage('Admin girişi yapmalısın.');return;}
  const email=normalizeEmail(user.email);
  if(email!==ADMIN_EMAIL){showAdminDenied(email);return;}

  const card=document.querySelector(`[data-match-id="${matchId}"]`);
  if(!card){writeAdminDebug('Sonuç kaydedilemedi: maç kartı bulunamadı.');return;}
  const homeInput=card.querySelector('[data-role="admin-home-score"]');
  const awayInput=card.querySelector('[data-role="admin-away-score"]');
  const message=card.querySelector('[data-role="admin-match-message"]');
  const homeScore=homeInput?.value;
  const awayScore=awayInput?.value;
  if(homeScore==='' || awayScore==='' || homeScore===undefined || awayScore===undefined){
    if(message){message.textContent='Lütfen iki takımın skorunu da gir.';message.hidden=false;}
    return;
  }

  const numericHomeScore=Number(homeScore);
  const numericAwayScore=Number(awayScore);
  const {data,error}=await client
    .from('matches')
    .update({home_score:numericHomeScore,away_score:numericAwayScore,status:'finished'})
    .eq('id',matchId)
    .select();

  if(error){
    const errorMessage='Sonuç kaydedilemedi: '+error.message;
    writeAdminDebug(errorMessage);
    if(message){message.textContent=errorMessage;message.hidden=false;}
    return;
  }

  if(!data || data.length===0){
    const emptyMessage='Sonuç kaydedilemedi: maç bulunamadı.';
    writeAdminDebug(emptyMessage);
    if(message){message.textContent=emptyMessage;message.hidden=false;}
    return;
  }

  const pointResult=await calculatePointsForMatch(matchId,numericHomeScore,numericAwayScore);
  if(pointResult.error){
    const errorMessage='Sonuç kaydedilemedi: '+pointResult.error;
    writeAdminDebug(errorMessage);
    if(message){message.textContent=errorMessage;message.hidden=false;}
    return;
  }

  const successMessage=pointResult.hasPredictions?'Sonuç kaydedildi ve puanlar güncellendi.':'Sonuç kaydedildi. Bu maç için tahmin yok.';
  writeAdminDebug(successMessage);
  if(message){message.textContent=successMessage;message.hidden=false;}
  await loadAdminMatches();
}

async function resetTestData(){
  const client=getSupabaseClient();
  if(!client) return;
  if(!(await isAdminSession())){writeAdminDebug('Bu işlem için admin yetkisi gerekir.');return;}
  const confirmed=confirm('Bu işlem tüm maç sonuçlarını, tahmin puanlarını ve kullanıcı toplam puanlarını sıfırlayacak. Emin misin?');
  if(!confirmed) return;

  const {error:matchesError}=await client.from('matches').update({home_score:null,away_score:null,status:'scheduled'}).not('id','is',null);
  if(matchesError){writeAdminDebug('Test verileri sıfırlanamadı: '+matchesError.message);return;}

  const {error:predictionsError}=await client.from('match_predictions').update({points:0,calculated:false}).not('user_id','is',null);
  if(predictionsError){writeAdminDebug('Test verileri sıfırlanamadı: '+predictionsError.message);return;}

  const {error:profilesError}=await client.from('profiles').update({total_points:0}).not('id','is',null);
  if(profilesError){writeAdminDebug('Test verileri sıfırlanamadı: '+profilesError.message);return;}

  writeAdminDebug('Test verileri sıfırlandı.');
  await loadAdminMatches();
}

function showRankingsMessage(message){
  const box=document.getElementById('rankingsMessage');
  if(!box) return;
  box.textContent=message;
  box.hidden=!message;
}

function formatPoints(value){
  return Number(value || 0).toLocaleString('tr-TR')+' '+translate('points');
}

function createRankingRow(items){
  const row=document.createElement('li');
  row.className='rrow';
  items.forEach(item=>{
    const span=document.createElement('span');
    span.className=item.className;
    span.textContent=item.text;
    row.appendChild(span);
  });
  return row;
}

let cachedRankingProfiles=[];
let cachedCountryStats=[];
let individualExpandedMode='top100';
let countryParticipationExpanded=false;
let collectiveRankingExpanded=false;
let currentRankingUserId=null;

async function fetchProfilesForRankings(){
  const client=getSupabaseClient();
  if(!client) return [];
  const {data,error}=await client
    .from('profiles')
    .select('id,email,display_name,country,total_points,referral_code,referred_by,premium')
    .order('total_points',{ascending:false})
    .limit(1000);
  if(error) throw error;
  return data || [];
}

async function updateProfileRanks(profile){
  const worldRankEl=document.getElementById('profileWorldRank');
  const countryRankEl=document.getElementById('profileCountryRank');
  if(!worldRankEl && !countryRankEl) return;
  const profiles=await fetchProfilesForRankings();
  const sorted=[...profiles].sort((a,b)=>Number(b.total_points || 0)-Number(a.total_points || 0));
  const worldIndex=sorted.findIndex(item=>item.id===profile.id);
  if(worldRankEl) worldRankEl.textContent=worldIndex>=0?'#'+(worldIndex+1):'-';
  const countrySorted=sorted.filter(item=>canonicalCountry(item.country)===canonicalCountry(profile.country));
  const countryIndex=countrySorted.findIndex(item=>item.id===profile.id);
  if(countryRankEl) countryRankEl.textContent=countryIndex>=0?'#'+(countryIndex+1):'-';
}

function aggregateCountries(profiles){
  const map=new Map(WORLD_CUP_COUNTRIES.map(country=>[country,{country,count:0,total:0}]));
  profiles.forEach(profile=>{
    const country=canonicalCountry(profile.country || 'Belirtilmedi');
    if(!map.has(country)) map.set(country,{country,count:0,total:0});
    const item=map.get(country);
    item.count++;
    item.total+=Number(profile.total_points || 0);
  });
  return [...map.values()];
}

function getReferralCountForProfile(profile,profiles=cachedRankingProfiles){
  const code=profile?.referral_code;
  if(!code) return 0;
  return (profiles || []).filter(item=>String(item.referred_by || '').trim()===String(code).trim()).length;
}
function renderIndividualRows(list,profiles,limit){
  list.innerHTML='';
  profiles.slice(0,limit).forEach((profile,index)=>{
    const isCurrentUser=profile.id===currentRankingUserId;
    const row=createRankingRow([
      {className:'rbadge',text:String(index+1)},
      {className:'rname',text:formatDisplayName(profile.display_name)},
      {className:'rval',text:formatCountry(profile.country)},
      {className:'rval rval-g',text:formatPoints(profile.total_points)}
    ]);
    if(isCurrentUser){
      row.classList.add('rank-current-user');
      const tag=document.createElement('span');
      tag.className='rank-self-tag';
      tag.textContent=translate('selfTag');
      row.appendChild(tag);
    }
    list.appendChild(row);
  });
}

function renderCountryParticipationRows(list,countryStats,limit){
  list.innerHTML='';
  countryStats.slice(0,limit).forEach((item,index)=>{
    list.appendChild(createRankingRow([
      {className:'rbadge',text:String(index+1)},
      {className:'rname',text:formatCountry(item.country)},
      {className:'rval rval-g',text:item.count+' '+translate('user')}
    ]));
  });
}

function renderCollectiveRows(list,countryStats,limit){
  list.innerHTML='';
  countryStats.slice(0,limit).forEach((item,index)=>{
    list.appendChild(createRankingRow([
      {className:'rbadge',text:String(index+1)},
      {className:'rname',text:formatCountry(item.country)},
      {className:'rval',text:item.count+' '+translate('participant')},
      {className:'rval rval-g',text:formatPoints(item.total)}
    ]));
  });
}

function renderExpandedIndividualRanking(){
  const expanded=document.getElementById('individualRankingExpanded');
  const list=document.getElementById('individualRankingExpandedList');
  const button=document.getElementById('showAllIndividualRanking');
  if(!expanded || !list || !button) return;
  expanded.hidden=false;
  const limit=individualExpandedMode==='all'?cachedRankingProfiles.length:100;
  renderIndividualRows(list,cachedRankingProfiles,limit);
  button.hidden=individualExpandedMode==='all' || cachedRankingProfiles.length<=100;
  button.textContent=translate('showAllRanking');
}

function showMoreIndividualRanking(){
  const expanded=document.getElementById('individualRankingExpanded');
  if(expanded && !expanded.hidden){
    expanded.hidden=true;
    individualExpandedMode='top100';
    return;
  }
  individualExpandedMode='top100';
  renderExpandedIndividualRanking();
  document.getElementById('individualRankingExpanded')?.scrollIntoView({behavior:'smooth',block:'start'});
}

function showAllIndividualRanking(){
  individualExpandedMode='all';
  renderExpandedIndividualRanking();
}

function showMoreCountryParticipation(){
  const list=document.getElementById('countryParticipationList');
  const button=document.getElementById('showMoreCountryParticipation');
  if(!list || !button) return;
  countryParticipationExpanded=!countryParticipationExpanded;
  renderCountryParticipationRows(list,cachedCountryStats,countryParticipationExpanded?cachedCountryStats.length:10);
  button.textContent=countryParticipationExpanded?translate('closeList'):translate('showMore');
}

function showMoreCollectiveRanking(){
  const list=document.getElementById('collectiveRankingList');
  const button=document.getElementById('showMoreCollectiveRanking');
  if(!list || !button) return;
  collectiveRankingExpanded=!collectiveRankingExpanded;
  const collectiveStats=[...cachedCountryStats].sort((a,b)=>b.total-a.total || b.count-a.count || a.country.localeCompare(b.country,'tr'));
  renderCollectiveRows(list,collectiveStats,collectiveRankingExpanded?collectiveStats.length:10);
  button.textContent=collectiveRankingExpanded?translate('closeList'):translate('showMore');
}

async function loadRankings(){
  const participationList=document.getElementById('countryParticipationList');
  const individualList=document.getElementById('individualRankingList');
  const collectiveList=document.getElementById('collectiveRankingList');
  if(!participationList || !individualList || !collectiveList) return;

  try{
    showRankingsMessage('');
    const rankingUser=await getCurrentUser();
    currentRankingUserId=rankingUser?.id || null;
    cachedRankingProfiles=await fetchProfilesForRankings();
    cachedRankingProfiles=[...cachedRankingProfiles].sort((a,b)=>Number(b.total_points || 0)-Number(a.total_points || 0));
    cachedCountryStats=aggregateCountries(cachedRankingProfiles).sort((a,b)=>b.count-a.count || formatCountry(a.country).localeCompare(formatCountry(b.country),currentLanguage));
    const collectiveStats=[...cachedCountryStats].sort((a,b)=>b.total-a.total || b.count-a.count || formatCountry(a.country).localeCompare(formatCountry(b.country),currentLanguage));

    individualList.innerHTML='';
    participationList.innerHTML='';
    collectiveList.innerHTML='';

    countryParticipationExpanded=false;
    collectiveRankingExpanded=false;
    individualExpandedMode='top100';
    const expanded=document.getElementById('individualRankingExpanded');
    if(expanded) expanded.hidden=true;

    if(cachedRankingProfiles.length===0){
      showRankingsMessage(translate('noRankingData'));
    }

    renderIndividualRows(individualList,cachedRankingProfiles,10);
    renderCountryParticipationRows(participationList,cachedCountryStats,10);
    renderCollectiveRows(collectiveList,collectiveStats,10);

    const individualButton=document.getElementById('showMoreIndividual');
    const countryButton=document.getElementById('showMoreCountryParticipation');
    const collectiveButton=document.getElementById('showMoreCollectiveRanking');
    if(individualButton){individualButton.hidden=cachedRankingProfiles.length<=10;individualButton.textContent=translate('showMore');}
    if(countryButton){countryButton.hidden=false;countryButton.textContent=translate('showMore');}
    if(collectiveButton){collectiveButton.hidden=false;collectiveButton.textContent=translate('showMore');}
  }catch(error){
    console.error('Sıralama yüklenemedi:',error);
    showRankingsMessage('Sıralama yüklenemedi: '+(error?.message || 'Bilinmeyen hata'));
  }
}
async function signUpUser(){
  const client=getSupabaseClient();
  if(!client) return;
  const email=document.getElementById('signupEmail')?.value.trim();
  const password=document.getElementById('signupPassword')?.value;
  const country=document.getElementById('signupCountry')?.value;
  const referredBy=document.getElementById('signupReferral')?.value.trim() || null;
  const displayName=generatePlayerName();

  const {data,error}=await client.auth.signUp({
    email,
    password,
    options:{data:{country}}
  });
  if(error){showAuthMessage(formatAuthError(error));return;}

  const user=data.user;
  if(user){
    const {error:profileError}=await client.from('profiles').upsert({
      id:user.id,
      email:user.email,
      display_name:displayName,
      country,
      premium:false,
      total_points:0,
      referral_code:generateReferralCode(),
      referred_by:referredBy,
      created_at:new Date().toISOString()
    });
    if(profileError){showAuthMessage(profileError.message);return;}
  }

  showAuthMessage('');
  closeAuthModal();
  applyLanguage();
  await updateHeaderAuthState();
  window.location.href='profile.html';
}

async function loginUser(){
  const client=getSupabaseClient();
  if(!client) return;
  const email=document.getElementById('loginEmail')?.value.trim();
  const password=document.getElementById('loginPassword')?.value;
  const {error}=await client.auth.signInWithPassword({email,password});
  if(error){showAuthMessage(formatAuthError(error));return;}
  showAuthMessage('');
  closeAuthModal();
  applyLanguage();
  await updateHeaderAuthState();
  window.location.href='profile.html';
}

async function logoutUser(){
  const client=getSupabaseClient();
  if(client){await client.auth.signOut();}
  window.location.href='index.html';
}

async function createOrLoadProfile(){
  const loggedOut=document.getElementById('loggedOutProfile');
  const loggedIn=document.getElementById('loggedInProfile');

  const client=getSupabaseClient();
  if(!client){
    if(loggedOut) loggedOut.hidden=false;
    if(loggedIn) loggedIn.hidden=true;
    return;
  }

  const user=await getCurrentUser();
  if(!user){
    if(loggedOut) loggedOut.hidden=false;
    if(loggedIn) loggedIn.hidden=true;
    return;
  }

  const profile=await loadProfileForUser(user);
  if(!profile) return;

  if(loggedOut) loggedOut.hidden=true;
  if(loggedIn) loggedIn.hidden=false;
  fillProfileFields(profile,user);
  await updateProfileRanks(profile);
}

async function updateHeaderAuthState(){
  const user=await getCurrentUser();
  document.querySelectorAll('.auth-logged-out').forEach(el=>el.hidden=!!user);
  document.querySelectorAll('.auth-logged-in').forEach(el=>el.hidden=!user);
}

window.signUpUser=signUpUser;
window.loginUser=loginUser;
window.logoutUser=logoutUser;
window.getCurrentUser=getCurrentUser;
window.createOrLoadProfile=createOrLoadProfile;
window.updateHeaderAuthState=updateHeaderAuthState;
window.showAuthTab=showAuthTab;
window.openAuthModal=openAuthModal;
window.closeAuthModal=closeAuthModal;
window.handlePredictionStart=handlePredictionStart;
window.saveTournamentPredictions=saveTournamentPredictions;
window.loadTournamentPredictions=loadTournamentPredictions;
window.loadMatches=loadMatches;
window.saveMatchPrediction=saveMatchPrediction;
window.loadAdminPanel=loadAdminPanel;
window.saveMatchResult=saveMatchResult;
window.adminLogin=adminLogin;
window.resetTestData=resetTestData;
window.loadRankings=loadRankings;
window.setLanguage=setLanguage;

window.addEventListener('scroll',()=>hdr.classList.toggle('sc',window.scrollY>40),{passive:true});
hbg.addEventListener('click',e=>{e.stopPropagation();const o=mob.classList.toggle('o');hbg.classList.toggle('o',o)});
document.addEventListener('click',()=>{mob.classList.remove('o');hbg.classList.remove('o')});
mob.addEventListener('click',e=>e.stopPropagation());
mob.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{mob.classList.remove('o');hbg.classList.remove('o')}));
if(showAllMatchesBtn){
  showAllMatchesBtn.addEventListener('click',()=>{
    document.querySelectorAll('.extra-match').forEach(match=>match.hidden=false);
    showAllMatchesBtn.hidden=true;
  });
}

document.getElementById('signupForm')?.addEventListener('submit',e=>{e.preventDefault();signUpUser();});
document.getElementById('loginForm')?.addEventListener('submit',e=>{e.preventDefault();loginUser();});
document.getElementById('adminLoginForm')?.addEventListener('submit',e=>{e.preventDefault();adminLogin();});
document.getElementById('resetTestDataButton')?.addEventListener('click',resetTestData);
document.getElementById('showMoreIndividual')?.addEventListener('click',showMoreIndividualRanking);
document.getElementById('showAllIndividualRanking')?.addEventListener('click',showAllIndividualRanking);
document.getElementById('showMoreCountryParticipation')?.addEventListener('click',showMoreCountryParticipation);
document.getElementById('showMoreCollectiveRanking')?.addEventListener('click',showMoreCollectiveRanking);
document.getElementById('saveTournamentPredictions')?.addEventListener('click',e=>{e.preventDefault();saveTournamentPredictions();});
document.addEventListener('keydown',e=>{if(e.key==='Escape') closeAuthModal();});

window.addEventListener('DOMContentLoaded',async()=>{
  if(document.getElementById('authModal')){
    const authParam=new URLSearchParams(location.search).get('auth');
    const requestedAuth=authParam==='signup' || location.hash==='#signup' ? 'signup' : 'login';
    showAuthTab(requestedAuth);
    if(location.hash==='#login' || location.hash==='#signup' || authParam==='login' || authParam==='signup'){
      openAuthModal(requestedAuth);
    }
  }
  applyLanguage();
  await updateHeaderAuthState();
  await createOrLoadProfile();
  await loadTournamentPredictions();
  await loadMatches();
  await loadAdminPanel();
  await loadRankings();
});
